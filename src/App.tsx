import * as React from "react";
import { AppLayout, NavBar, SceneSetup } from "components";

const App: React.FC = () => (
  <AppLayout>
    <NavBar />
    <SceneSetup />
  </AppLayout>
);

export default App;
